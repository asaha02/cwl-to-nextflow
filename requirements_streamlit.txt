# Streamlit App Requirements
# Core dependencies for the CWL to Nextflow Migration Toolkit Streamlit app

# Streamlit and web interface
streamlit>=1.28.0
streamlit-option-menu>=0.3.6
streamlit-ace>=0.1.1

# Core dependencies
# Note: cwltool is optional but recommended for full CWL parsing
# Install with: pip install cwltool
# Or: pip install cwltool>=3.1.20231201152605

# YAML processing (ruamel.yaml is preferred, but pyyaml works as fallback)
ruamel.yaml>=0.17.32
pyyaml>=6.0.1

# HTTP and AWS
requests>=2.31.0
boto3>=1.34.0
botocore>=1.34.0

# CWL processing (optional - install if you need full CWL validation)
# cwl-utils>=0.25
# schema-salad>=8.4.20231201152605

# Nextflow generation
jinja2>=3.1.2
pyyaml>=6.0.1
nextflow>=22.04.0

# AWS HealthOmics
amazon-omics-tools

# Validation and testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Additional utilities for Streamlit
plotly>=5.15.0
pandas>=2.0.0
numpy>=1.24.0

# File handling
python-magic>=0.4.27
python-magic-bin>=0.4.14; platform_system == "Windows"
